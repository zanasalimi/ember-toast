# embertoast — environment

# The library itself needs no environment. These values configure the docs/playground app only.

# Public base URL used for canonical links and the OG image. No trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Port the docs container binds. Compose maps this to the host.
PORT=3000
